version: "3"

tasks:
  # Task to start the Node.js timer service
  start-timer-service:
    desc: Run the Node timer service
    cmds:
      - node timer_service/server.js

  start:
    desc: Run the Node timer service
    cmds:
      - task: start-timer-service
